
## Note
git has content that cannot be grepped/parsed because it is stored in a packed format. 
eg. messages of tags

git useful commands


git branch --show-current: current branch

git pull (branch name/overwise main) 

 ```
git diff --name-only --cached
```
see the name of all staged files